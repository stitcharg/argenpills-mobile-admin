{
  "name": "argenpils-admin",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@emotion/react": "^11.11.1",
    "@emotion/styled": "^11.11.0",
    "@mui/material": "^5.14.8",
    "@mui/styled-engine-sc": "^5.14.8",
    "ra-data-json-server": "^4.13.3",
    "react": "^18.2.0",
    "react-admin": "^4.13.3",
    "react-dom": "^18.2.0",
    "react-ga": "^3.3.1",
    "react-scripts": "5.0.1",
    "styled-components": "^5.3.11",
    "web-vitals": "^3.4.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build:production": "NODE_ENV=production react-scripts build",
    "upload:production": "aws s3 sync build s3://argenpills-admin --profile ap && aws cloudfront create-invalidation --distribution-id EBTQACRFANAZ3 --paths '/*' --profile ap"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
